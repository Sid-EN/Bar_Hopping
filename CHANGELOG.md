# 版本紀錄

TAIWAN BAR BIBLE 的版本沿革。只記大版本與小版本；單純新增／修正幾間酒吧、
調整文案這類微更新不另立版本，會併入下一個版本說明。

版本號規則：

- **大版本**（x.0.0）：改變了資料的存放方式或既有用法會失效的變更
- **小版本**（3.x.0）：新增功能、明顯的效能改善、資料量級的躍進
- 每個版本對應一個 git tag，可用 `git show v3.7.0` 看當時的完整內容

---

## v3.8.0 — 跑吧四件事：距離範圍、路線分享、指定時段、最近捷運站

**2026-09-02**

四個都是為了同一個場景：**人已經在外面了，接下來去哪。**

### 📏 距離範圍

新增「只看 500 公尺／1／2／5 公里內」。先前有「離我最近」排序，
但你得自己往下捲看距離數字才知道哪些走得到。

沒定位時選了範圍不會靜靜地不生效——欄位下方會說明原因，
而且選了範圍會順手把定位叫出來。沒有座標的店在設了範圍時會排除
（算不出距離），提示文字會寫明有幾間因此看不到。

### 🔗 今晚路線可以分享

網址分享原本只帶篩選條件，現在**排好的路線也一起帶著走**——
「我排好五攤了，你看一下」變成一個連結的事。

- 用 `barKey`（店名｜縣市）而不是陣列索引：索引會隨 `data.js` 增修而位移，
  昨天分享的連結明天就指到別間店了
- 對方已經有路線時會先問過再覆蓋
- 連結裡有已改名或移除的店就略過那幾筆，其餘照常載入——
  五攤裡有一攤消失，另外四攤仍然有用

### 🕐 指定時段

填一個時間，只看那時營業的店。「我 11 點才出門」「凌晨 1 點還想喝」
先前只能靠「現在還有開」和固定的「營業到 3 點後」標籤湊。

哪一天要看跑吧的作息而不是日曆：**中午 12 點為界**，填 23:00 是今晚、
填 01:00 是今晚喝到隔天的那個凌晨——與「今晚路線每天中午重設」同一套規則。
換算後的日期會直接寫在欄位下方，不用猜。

解析不出營業時間的店會被排除：這裡問的是「那時能不能去」，
「時間未知」不是肯定的答案。

### 🚇 最近捷運站

卡片與詳細頁顯示「捷運頂埔站 320 公尺・步行約 4 分」，
並新增「🚇 近捷運站」標籤（站旁 500 公尺內）。

車站資料放在新檔 `stations.js`，由 `tools/fetch_stations.js` 從
OpenStreetMap 產生，共 273 站，不需金鑰也沒有額度限制。
排除桃園機場的 Skytrain（航廈接駁電車，對跑吧沒有意義）。
超過 1.2 公里就不顯示——走路 15 分鐘以上的話這個資訊已經不影響決定。

實際覆蓋：台北 550/566（97%）、新北 178/193、高雄 185/216、台中 92/212。
台南、新竹、嘉義是 0，因為這些城市沒有捷運。

---

## v3.7.0 — 分頁與每頁筆數選擇

**2026-09-02**

清單改為分頁，並讓使用者自己決定一頁看幾筆。

- 每頁筆數可選 **10／30／50／100**（預設 50），選擇會記在瀏覽器裡，下次開還在
- 分頁列有上一頁／下一頁與頁碼，頁碼會摺疊（`1 … 11 12 13 … 41`），
  兩千間也不會排出四十顆按鈕
- 顯示「第 51–100 間，共 2,002 間（第 2 / 41 頁）」
- 換每頁筆數時會停在原本看的那一筆附近，不會被丟回第一頁
- 換篩選條件會回到第一頁；每分鐘的營業狀態刷新則保留目前頁碼

取代 v3.6.0 的無限捲動。分頁對「我看到第幾間了」這件事比較好掌握，
也可以直接跳到中後段，不用一直往下捲。

效能（2,002 間，jsdom 實測）：

| 每頁筆數 | 一次篩選 | DOM 節點數 |
|---|---|---|
| 10 | 3.6 ms | 159 |
| 30 | 8.0 ms | 453 |
| 50 | 10.3 ms | 760 |
| 100 | 21.1 ms | 1,533 |
| （改版前一次全塞） | 526 ms | 33,337 |

---

## v3.6.0 — 效能：清單分批渲染

**2026-09-02**

資料到兩千間之後，整個網頁操作明顯卡頓。量測後確認瓶頸不在資料載入，
而在一次把全部卡片建成 DOM：產生 2,000 張卡片的 HTML 字串只要約 11 毫秒，
但瀏覽器要把三萬多個節點解析、排版、繪製出來要 500 毫秒以上——
而且每改一次篩選、每打一個字都要整份重來。

- 清單改為先渲染 60 張，捲到底再補下一批
- 搜尋框加上 180 毫秒 debounce
- 每分鐘的營業狀態刷新保留已捲出的批次，不會把人拉回開頭

改一次篩選條件從 526 ms 降到 16 ms，首屏 DOM 節點數從 33,337 降到 906。

> 這個版本的無限捲動在 v3.7.0 改成了分頁。

---

## v3.5.0 — Asia's 50 Best Bars 2026 核對與獎項修正

**2026-09-02**

對照官方 1–50 與 51–100 兩份名單逐筆核對，台灣 8 間全部已收錄且名次正確
（Vender #14、To Infinity & Beyond #35、The Han-Jia #41、Moonrock #67、
Maltail #68、Bar Mood #82、The Public House #92、Lab #99）。

核對過程中修掉三類錯誤：

- **INDULGE 的名次年份標錯**：「Asia's 50 Best Bars 2024 #11」實際是 2023 年的名次，
  2024 年榜上並沒有 INDULGE
- **五間店的 Taiwan Bar Awards 名次自相矛盾**：同時掛著正確名次與一筆多餘的
  「Top 20」，對照第六屆官方榜單後移除，並補上 Moonrock 的 Top 1
- **19 筆重複**：其中 12 筆是「只有店名、沒有地址」的骨架記錄，
  與後來抓到的完整記錄重複（例如「BAR MOOD」對「Bar Mood Taipei 吧沐」）。
  這類重複原本的同址比對抓不到——骨架根本沒有地址可比

收錄數 2,000 → 2,002。

---

## v3.4.0 — 擴充至 2,000 間，並修復測試的 heap 溢位

**2026-09-02**

- 收錄數 1,536 → 2,000 間，涵蓋 22 縣市
- 補上 2026 年獎項；移除 7 筆誤標的「Taiwan Bar Awards 2026」
  （第六屆雖在 2026-01 公布，頒的是 2025 年度的獎）

**測試記憶體修復**：功能測試會開二十幾個 jsdom，每個都渲染全部酒吧，
1,536 間時整份測試就 heap 溢位。量測後確認 jsdom 的 `close()` 延到 macrotask
才釋放，而測試檔全同步跑、從頭到尾沒讓出過事件迴圈，每次 boot 都留著約 158 MB。

- 每個測試區塊各自包成 async function（包在同一個大的 async function 裡沒用：
  V8 會把 `await` 前後的區塊變數提升到該函式 context，整份跑完前都不釋放）
- `section()` 關掉前一批 jsdom → 讓出事件迴圈 → 主動 GC，
  檔案開頭自己用 `--expose-gc` 重跑一次
- 新增記憶體守門測試，斷言峰值低於每間 1 MB

---

## v3.3.0 — 擴充至 1,536 間，補上獲獎紀錄

**2026-09-02**

- 收錄數 1,003 → 1,536 間
- 補上 2024／2025 年的 Asia's 50 Best Bars 與 Taiwan Bar Awards 獲獎紀錄
- 2023 年的名次因來源彼此矛盾，一律不記錄

---

## v3.2.0 — 突破千間

**2026-09-02**

收錄數 590 → 1,003 間。

---

## v3.1.0 — 連線穩定性與路線重設

**2026-09-01**

- **修正「讀不到 data.js (401)」**：根因是 Service Worker 對所有跨網域 GET
  採快取優先，連 `api.github.com` 的 401 回應都存了起來，換再多把新權杖
  都還是回同一個錯誤。API 請求與帶授權標頭的請求一律放行不攔截，
  且只快取成功的回應
- 精簡連線設定介面，權杖到期日改為使用者填寫、可檢視，測試連線改為逐關診斷
- **今晚路線每天中午 12:00 自動重設**（用中午當界線，跨夜跑到凌晨也不會
  喝到一半清單就沒了）
- 個人酒單可同步

---

## v3.0.0 — 資料改以 repo 為準

**2026-09-01**

> **破壞性變更**：移除「儲存到這台裝置」的本機自訂酒吧機制。
> 先前存在瀏覽器裡的自訂酒吧不再顯示。

- 表單直接編輯 repo 的 `data.js`，寫回成功後同步更新記憶體資料，
  畫面立即反映，不必等 Pages 重新部署
- localStorage 只留收藏／打卡／筆記／路線
- 詳細頁的「編輯資料」開放給所有酒吧，不再只限自己新增的
- 編輯模式新增「從 repo 刪除」
- 改名時會一併搬移收藏、打卡與路線的對應紀錄
- 加強重複偵測與權杖處理

---

## v2.0.0 — 網頁端新增／編輯酒吧

**2026-09-01**

不必動 repo 也能從網頁新增酒吧。

- 表單涵蓋全部欄位，只有店名與縣市必填，留空不會存成空字串
- 即時驗證，規則與 `tests/validate-data.js` 一致，避免存進去的資料在 CI 被擋
- 營業時間欄位即時回饋解析結果
- 邊填邊顯示卡片預覽
- 「複製 data.js 格式」產生可直接貼進 repo 的一行

---

## v1.4.0 — 定位、時間推算與個人化紀錄

**2026-09-01**

- **離我最近**：瀏覽器定位後依距離排序，顯示「480 公尺・步行約 6 分」，
  無座標的排最後
- **路線時間推算**：依出發時間、每攤停留 60 分鐘、步行 4.5 km/h 推算抵達時間，
  趕不上打烊或只剩不到一小時的直接標紅警告
- **今晚喝哪間**：從目前篩選結果隨機抽一間
- **個人筆記與到訪紀錄**：顯示「去過 3 次・上次 2 個月前」
- **跑吧回顧**：依年份統計攻略間數、踏足縣市、最常去的區域、平均給分與五星愛店
- 分享圖與淺色主題

---

## v1.3.0 — CI、資料校驗與自動部署

**2026-09-01**

- `tests/validate-data.js`：檢查欄位型別、縣市名稱、重複店名、座標範圍、
  營業時間能否被程式解析
- `tests/app.test.js`：用 jsdom 載入真實頁面跑完整使用者操作
- `tests/pwa.test.js`：檢查 manifest、Service Worker 與相對路徑
- 每次 push 與 PR 自動跑全部測試；推上 `main` 自動部署到 GitHub Pages

---

## v1.2.0 — 我的酒單、多選標籤與 PWA

**2026-09-01**

- **我的酒單**：分收藏／已攻略／今晚路線三個分頁，可直接移除項目、
  調整路線順序，支援匯出／匯入 JSON 備份
- **今晚路線**順序改由使用者掌控，每一攤可前後移動；自動排序改成按鈕
- **多選標籤篩選**取代原本單選的下拉：酒類複選為 OR，亮點複選為 AND
- 網址分享：篩選條件可帶在網址上
- PWA 離線可用

---

## v1.1.0 — 地圖、跑吧路線與收藏打卡

**2026-09-01**

- **現在還有開**：解析中文營業時間字串判斷當下狀態，會算還有多久打烊，
  跨夜、公休日、週末延長時段都能處理；解析不出來就標示「時間未知」不亂猜
- **地圖模式**：Leaflet + OSM 底圖，圖釘依獲獎／推薦／已喝過／收藏上色，
  跟著篩選連動
- **今晚路線**：最多 8 攤，有座標時用最近鄰排序並計算步行距離，
  一鍵開 Google Maps 多點導航
- **收藏與打卡**：收藏、標記已喝過、給個人評分，附進度條（存在 localStorage）
- 卡片加上 Google 五星評分與人均消費區間；已歇業店家會灰化並掛上警示標章

---

## v1.0.0 — 擴充為全台酒吧指南

**2026-08-31**

從台北／新北的個人清單擴充為全台指南。

- 酒吧資料獨立成 `data.js`，依縣市分段並加上欄位說明
- 原有 122 間台北／新北清單完整保留（含個人化註記），新增 100 間全台知名酒吧，
  共 222 間、20 縣市
- 21 間標註國際獎項（Asia's 50 Best Bars、Spirited Awards 等）
- 縣市分區頁籤（北／中／南／東部離島），行政區選單隨所選縣市動態連動
- 新增獲獎名店、脆友推薦、價位篩選與四種排序
- 點擊卡片開啟詳細頁，含完整資訊、獎項與 Google 地圖

---

## v0.1.0 — 初版

**2026-04-08**

單一 `index.html`，台北／新北的個人酒吧清單。
